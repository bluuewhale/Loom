---
phase: 04-performance-hardening-benchmark-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graph/testdata/football.go
  - graph/testdata/polbooks.go
  - graph/testhelpers_test.go
  - graph/accuracy_test.go
  - graph/louvain_test.go
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "Football fixture has 115 nodes and 613 edges"
    - "Polbooks fixture has 105 nodes and 441 edges"
    - "nmi() helper is accessible from all test files in package graph"
    - "Louvain and Leiden both achieve NMI validation on Football and Polbooks"
    - "Louvain and Leiden both achieve Q > 0.35 on Karate Club"
    - "All 8 edge cases pass for both Louvain and Leiden"
  artifacts:
    - path: "graph/testdata/football.go"
      provides: "Football 115-node fixture edges + ground-truth partition"
      contains: "FootballEdges"
    - path: "graph/testdata/polbooks.go"
      provides: "Polbooks 105-node fixture edges + ground-truth partition"
      contains: "PolbooksEdges"
    - path: "graph/testhelpers_test.go"
      provides: "Shared nmi() and uniqueCommunities helpers"
      contains: "func nmi"
    - path: "graph/accuracy_test.go"
      provides: "NMI accuracy tests for Football and Polbooks"
      contains: "TestFootball"
    - path: "graph/louvain_test.go"
      provides: "Complete 8 edge-case coverage for Louvain"
      contains: "TestLouvainCompleteGraph"
  key_links:
    - from: "graph/accuracy_test.go"
      to: "graph/testdata/football.go"
      via: "testdata.FootballEdges"
      pattern: "testdata\\.FootballEdges"
    - from: "graph/accuracy_test.go"
      to: "graph/testdata/polbooks.go"
      via: "testdata.PolbooksEdges"
      pattern: "testdata\\.PolbooksEdges"
    - from: "graph/accuracy_test.go"
      to: "graph/testhelpers_test.go"
      via: "nmi() call"
      pattern: "nmi\\(res\\.Partition"
---

<objective>
Add Football (115-node) and Polbooks (105-node) benchmark fixtures, extract shared test helpers, create NMI accuracy tests for all three benchmark graphs, and complete the 8-edge-case coverage for Louvain.

Purpose: Validates algorithm correctness on standard community detection benchmarks beyond Karate Club. Ensures both algorithms handle all edge cases.
Output: Two fixture files, shared test helpers, accuracy test suite, Louvain edge-case additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance-hardening-benchmark-fixtures/04-RESEARCH.md

@graph/testdata/karate.go
@graph/louvain_test.go
@graph/leiden_test.go
@graph/detector.go

<interfaces>
<!-- Key types and contracts the executor needs -->

From graph/detector.go:
```go
type CommunityDetector interface {
    Detect(g *Graph) (CommunityResult, error)
}
type CommunityResult struct {
    Partition  map[NodeID]int
    Modularity float64
    Passes     int
    Moves      int
}
func NewLouvain(opts LouvainOptions) CommunityDetector
func NewLeiden(opts LeidenOptions) CommunityDetector
```

From graph/testdata/karate.go (fixture pattern):
```go
package testdata
var KarateClubEdges = [][2]int{ ... }
var KarateClubPartition = map[int]int{ ... }
```

From graph/leiden_test.go (nmi helper to extract):
```go
func nmi(p1, p2 map[NodeID]int) float64
func uniqueCommunities(partition map[NodeID]int) int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Football + Polbooks fixtures, extract shared test helpers</name>
  <files>graph/testdata/football.go, graph/testdata/polbooks.go, graph/testhelpers_test.go, graph/leiden_test.go, graph/louvain_test.go</files>
  <read_first>graph/testdata/karate.go, graph/leiden_test.go, graph/louvain_test.go</read_first>
  <action>
1. Create `graph/testdata/football.go`:
   - Package `testdata`
   - `var FootballEdges = [][2]int{ ... }` with all 613 edges of the Girvan-Newman football network (115 nodes, IDs 0-114). Source the edge list from the standard GML dataset (Girvan & Newman, 2002). The network represents American college football games in Fall 2000 with 12 conferences as ground-truth communities.
   - `var FootballPartition = map[int]int{ ... }` with 12 ground-truth conference assignments (community IDs 0-11).
   - Add a comment header citing the source.

2. Create `graph/testdata/polbooks.go`:
   - Package `testdata`
   - `var PolbooksEdges = [][2]int{ ... }` with all 441 edges of the political books network (105 nodes, IDs 0-104). Source from Krebs (2004) — co-purchasing network of US political books.
   - `var PolbooksPartition = map[int]int{ ... }` with 3 ground-truth communities (liberal=0, neutral=1, conservative=2).
   - Add a comment header citing the source.

3. Create `graph/testhelpers_test.go` (package graph):
   - Move `nmi()` from `leiden_test.go` to this file (exact same implementation).
   - Move `uniqueCommunities()` from `louvain_test.go` to this file.
   - Add `buildGraph(edges [][2]int) *Graph` helper that creates an undirected graph from an edge list (used by accuracy tests).
   - Add `groundTruthPartition(gt map[int]int) map[NodeID]int` helper that converts `map[int]int` to `map[NodeID]int`.

4. Remove `nmi()` from `leiden_test.go` and `uniqueCommunities()` from `louvain_test.go` (they now live in testhelpers_test.go). Both files already import the testdata package. Ensure no compilation errors.

5. Verify: `go test ./graph/... -count=1` passes (all existing tests still work after refactor).
  </action>
  <verify>
    <automated>cd /Users/donghyungko/.superset/worktrees/loom/feat/community-detection && go test ./graph/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - graph/testdata/football.go exists with `FootballEdges` (613 edges) and `FootballPartition` (115 entries, 12 communities)
    - graph/testdata/polbooks.go exists with `PolbooksEdges` (441 edges) and `PolbooksPartition` (105 entries, 3 communities)
    - graph/testhelpers_test.go exists with `nmi()`, `uniqueCommunities()`, `buildGraph()`, `groundTruthPartition()`
    - `nmi()` removed from leiden_test.go, `uniqueCommunities()` removed from louvain_test.go
    - `go test ./graph/... -count=1` passes with zero failures
  </acceptance_criteria>
  <done>Football and Polbooks fixtures exist with correct node/edge counts, shared helpers extracted, all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Accuracy test suite + Louvain edge-case completion</name>
  <files>graph/accuracy_test.go, graph/louvain_test.go</files>
  <read_first>graph/testhelpers_test.go, graph/testdata/football.go, graph/testdata/polbooks.go, graph/louvain_test.go, graph/leiden_test.go</read_first>
  <action>
1. Create `graph/accuracy_test.go` (package graph):
   - `TestLouvainFootballNMI`: build Football graph via `buildGraph(testdata.FootballEdges)`, run `NewLouvain(LouvainOptions{Seed: 42}).Detect(g)`, assert Q > 0.0, assert `nmi(res.Partition, groundTruthPartition(testdata.FootballPartition)) >= 0.5`. Log actual Q, NMI, community count. (TEST-02)
   - `TestLeidenFootballNMI`: same as above with `NewLeiden(LeidenOptions{Seed: 2})`, assert NMI >= 0.5. (TEST-02)
   - `TestLouvainPolbooksNMI`: build Polbooks graph, run Louvain(Seed: 42), assert Q > 0.0, NMI >= 0.5. (TEST-03)
   - `TestLeidenPolbooksNMI`: same with Leiden(Seed: 2), NMI >= 0.5. (TEST-03)
   - `TestLouvainKarateClubNMI`: build Karate Club graph, run Louvain(Seed: 42), assert Q > 0.35, NMI >= 0.7 vs KarateClubPartition. (TEST-01 — adds NMI assertion to existing Q test)
   - `TestLeidenKarateClubNMI` is already covered by `TestLeidenKarateClubAccuracy` in leiden_test.go. Do NOT duplicate. (TEST-01)
   - Use conservative NMI thresholds (>= 0.5 for Football/Polbooks) with fixed seeds to avoid flaky tests.

2. Add missing Louvain edge cases to `graph/louvain_test.go` (TEST-04):
   Audit existing tests — Louvain has: empty, single-node, directed, disconnected-nodes, two-node, two-disconnected-triangles, partition-normalized, deterministic, two-triangles-connected.
   Missing from 8-case spec: giant+singletons, zero-resolution, complete graph, self-loop.
   Add:
   - `TestLouvainGiantPlusSingletons`: 3-node triangle + 2 isolated nodes. Assert no error, triangle nodes in same community, singletons each in own community.
   - `TestLouvainZeroResolution`: Karate Club with Resolution=0.0 (defaults to 1.0 per LouvainOptions docs). Verify same behavior as default. Also test with Resolution=0.001 (very low) — should merge everything into fewer communities.
   - `TestLouvainCompleteGraph`: 5-node complete graph. Assert no error, all nodes in same community, Q = 0 or slightly negative.
   - `TestLouvainSelfLoop`: graph with self-loop edge (node 0 -> node 0) plus a normal edge (0-1). Assert no error, valid partition.

3. Verify all tests pass: `go test ./graph/... -v -count=1`.
  </action>
  <verify>
    <automated>cd /Users/donghyungko/.superset/worktrees/loom/feat/community-detection && go test ./graph/... -v -run "TestLouvainFootball|TestLeidenFootball|TestLouvainPolbooks|TestLeidenPolbooks|TestLouvainKarateClubNMI|TestLouvainGiant|TestLouvainZeroRes|TestLouvainComplete|TestLouvainSelfLoop" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - graph/accuracy_test.go exists with 5 NMI accuracy tests (Louvain+Leiden x Football+Polbooks, plus LouvainKarateClubNMI)
    - All NMI tests pass with thresholds: Karate >= 0.7, Football >= 0.5, Polbooks >= 0.5
    - graph/louvain_test.go has TestLouvainGiantPlusSingletons, TestLouvainZeroResolution, TestLouvainCompleteGraph, TestLouvainSelfLoop
    - `go test ./graph/... -count=1` passes with zero failures
    - `go test -race ./graph/... -count=1` passes with zero race reports
  </acceptance_criteria>
  <done>Accuracy tests validate NMI on 3 benchmark graphs for both algorithms; all 8 Louvain edge cases covered; all tests pass including -race</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
go test ./graph/... -count=1

# Race-free
go test -race ./graph/... -count=1

# Fixture sanity: node/edge counts
go test ./graph/... -v -run "TestLouvainFootball|TestLeidenFootball|TestLouvainPolbooks|TestLeidenPolbooks" -count=1
```
</verification>

<success_criteria>
- Football fixture: 115 nodes, 613 edges, 12-community ground truth
- Polbooks fixture: 105 nodes, 441 edges, 3-community ground truth
- NMI validation passes for both algorithms on all 3 benchmarks
- All 8 edge cases pass for Louvain (was partial, now complete)
- All 8 edge cases pass for Leiden (already complete, verified)
- `go test -race ./graph/... -count=1` green
</success_criteria>

<output>
After completion, create `.planning/phases/04-performance-hardening-benchmark-fixtures/04-01-SUMMARY.md`
</output>
