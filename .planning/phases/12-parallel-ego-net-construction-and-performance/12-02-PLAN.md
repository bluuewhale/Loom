---
phase: 12-parallel-ego-net-construction-and-performance
plan: "02"
subsystem: graph/benchmark
type: auto
autonomous: true
wave: 2
depends_on: ["12-01"]
requirements: []
---

# Phase 12 Plan 02: Speedup Assertion Tests

## Objective

Add `BenchmarkDetect`, `BenchmarkUpdate1Node`, `BenchmarkUpdate1Edge` benchmarks and
`TestUpdate1NodeSpeedup` / `TestUpdate1EdgeSpeedup` speedup assertion tests to
`graph/benchmark_test.go`. Also add build-tagged `raceEnabled` const files so
performance tests can skip cleanly under `-race`.

## Context

Wave 1 (12-01) is complete on main. This worktree (feat/online-ego-splitting-framework)
has the Phase 11 incremental Update() implementation but lacks the benchmark additions
from 12-01.

- `BenchmarkUpdate1Node`: Update with 1 isolated new node (delta={node 34})
- `BenchmarkUpdate1Edge`: Update with edge 16↔24 (7 affected nodes)
- `TestUpdate1NodeSpeedup`: assert ≥10x speedup vs BenchmarkDetect
- `TestUpdate1EdgeSpeedup`: assert ≥1.5x speedup (ONLINE-09 documented limitation)

## Tasks

### Task 1: Add raceEnabled build-tag files

Create `graph/race_test.go` and `graph/norace_test.go` providing `const raceEnabled bool`
via build tags.

**Done:** Both files exist with correct build tags and compile.

### Task 2: Add benchmarks and speedup tests

Add to `graph/benchmark_test.go`:
- `BenchmarkDetect`: full Detect on 35-node graph (KarateClub + isolated node 34)
- `BenchmarkUpdate1Node`: Update with delta AddedNodes=[34]
- `BenchmarkUpdate1Edge`: Update with delta AddedEdges=[{16,24,1.0}]
- `TestUpdate1NodeSpeedup`: ≥10x gate, skip under race
- `TestUpdate1EdgeSpeedup`: ≥1.5x regression guard, skip under race

**Done:** All 5 benchmarks/tests compile and pass.

## Success Criteria

- [ ] `race_test.go` and `norace_test.go` created with correct build tags
- [ ] BenchmarkDetect, BenchmarkUpdate1Node, BenchmarkUpdate1Edge present
- [ ] TestUpdate1NodeSpeedup asserts ≥10x
- [ ] TestUpdate1EdgeSpeedup asserts ≥1.5x
- [ ] `go test ./graph/... -count=1 -run TestUpdate1` passes
