---
phase: quick
plan: 260331-id1
type: execute
wave: 1
depends_on: []
files_modified:
  - graph/leiden.go
  - graph/example_test.go
  - graph/leiden_numruns_test.go
  - graph/accuracy_test.go
  - scripts/compare.py
  - README.md
  - bench-baseline.txt
autonomous: true
requirements: []
must_haves:
  truths:
    - "leiden.go line 87 has a comment explaining intentional silent error discard"
    - "example_test.go helper is named communitySet, not uniqueCommunities"
    - "bench-baseline.txt shows pkg: github.com/bluuewhale/loom/graph"
    - "leiden_numruns_test.go has a NumRuns=2 test case"
    - "accuracy_test.go Football and Polbooks NMI thresholds are >= 0.95"
    - "compare.py best_partition calls include random_state=42"
    - "README.md says ~56ms for Leiden 10K, not ~57ms"
  artifacts:
    - path: "graph/leiden.go"
      provides: "Comment on silent error discard"
    - path: "graph/example_test.go"
      provides: "Renamed communitySet helper"
    - path: "graph/leiden_numruns_test.go"
      provides: "NumRuns=2 test"
    - path: "graph/accuracy_test.go"
      provides: "Tightened NMI thresholds"
    - path: "scripts/compare.py"
      provides: "Reproducible random_state"
    - path: "README.md"
      provides: "Corrected timing"
    - path: "bench-baseline.txt"
      provides: "Regenerated with current module name"
  key_links: []
---

<objective>
Apply 7 code review fixes from PR #4: comment on silent error discard in leiden.go, rename uniqueCommunities to communitySet in example_test.go, regenerate bench-baseline.txt with correct module name, add NumRuns=2 test, tighten NMI thresholds, add random_state to compare.py, fix README timing.

Purpose: Address all code review feedback in a single atomic commit.
Output: All 7 files updated, tests passing.
</objective>

<context>
@graph/leiden.go
@graph/example_test.go
@graph/leiden_numruns_test.go
@graph/accuracy_test.go
@scripts/compare.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply code and doc fixes (6 of 7 items)</name>
  <files>graph/leiden.go, graph/example_test.go, graph/leiden_numruns_test.go, graph/accuracy_test.go, scripts/compare.py, README.md</files>
  <action>
1. **graph/leiden.go** (~line 87): On the `return bestResult, nil` line, add a comment above it:
   `// At least one run succeeded — return its result. We intentionally discard lastErr`
   `// because partial multi-run failures (some iterations succeed, others fail) should`
   `// not prevent returning the best successful result.`

2. **graph/example_test.go**: Rename the `uniqueCommunities` function (line 125) to `communitySet`. Update its doc comment from "uniqueCommunities returns the number of distinct community IDs" to "communitySet returns the set of distinct community IDs". Update the call site on line 74 from `uniqueCommunities(result.Partition)` to `communitySet(result.Partition)`.

3. **graph/leiden_numruns_test.go**: Add a new test function `TestLeidenNumRunsTwoExplicit` after `TestLeidenNumRunsOneIsEquivalent` (after line 72). Pattern it after `TestLeidenNumRunsThreeExplicit` but with `NumRuns: 2`. Assert Q > 0 and Partition has 34 nodes. Add a t.Logf line showing Q and community count.

4. **graph/accuracy_test.go**: Change NMI thresholds for the 4 Football/Polbooks tests:
   - `TestLouvainFootballNMI` line 43: `score < 0.5` -> `score < 0.95`
   - `TestLeidenFootballNMI` line 63: `score < 0.5` -> `score < 0.95`
   - `TestLouvainPolbooksNMI` line 83: `score < 0.5` -> `score < 0.95`
   - `TestLeidenPolbooksNMI` line 103: `score < 0.5` -> `score < 0.95`
   Also update the corresponding error message strings from `want >= 0.5` to `want >= 0.95`.
   Also update the test doc comments from `NMI >= 0.5` to `NMI >= 0.95`.

5. **scripts/compare.py**: In `_benchmark_louvain` function, add `random_state=42` to both `best_partition` calls:
   - Line 45 warmup: `community_louvain.best_partition(G, random_state=42)`
   - Line 46 timed: the lambda `community_louvain.best_partition(G, random_state=42)`

6. **README.md**: Two changes:
   - Line 10: `~57ms` -> `~56ms`
   - Line 150: `~57ms` -> `~56ms`
  </action>
  <verify>
    <automated>cd /Users/donghyungko/.superset/worktrees/Loom/feat/v1.0-debt/graph && go test -run "TestLeiden(NumRuns|Football|Polbooks)|TestLouvain(Football|Polbooks)" -v -count=1 ./...</automated>
  </verify>
  <done>All 6 source/doc changes applied; affected tests pass with tightened thresholds.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate bench-baseline.txt</name>
  <files>bench-baseline.txt</files>
  <action>
Run benchmarks from the graph directory and write output to bench-baseline.txt at repo root:
```
cd graph && go test -bench=. -benchmem -count=5 ./... 2>&1 | tee ../bench-baseline.txt
```
After generation, verify the file contains `pkg: github.com/bluuewhale/loom/graph` (not the stale `community-detection/graph`).
  </action>
  <verify>
    <automated>grep -q "pkg: github.com/bluuewhale/loom/graph" /Users/donghyungko/.superset/worktrees/Loom/feat/v1.0-debt/bench-baseline.txt && echo "OK: correct module name"</automated>
  </verify>
  <done>bench-baseline.txt regenerated with current module path; no stale `community-detection/graph` entries remain.</done>
</task>

</tasks>

<verification>
1. `cd graph && go test -v -count=1 ./...` — all tests pass including new NumRuns=2 and tightened NMI thresholds
2. `grep "communitySet" graph/example_test.go` — renamed function exists
3. `grep "uniqueCommunities" graph/example_test.go` — returns nothing (old name gone)
4. `grep "random_state=42" scripts/compare.py` — reproducible seed present
5. `grep "~56ms" README.md` — corrected timing
6. `grep "pkg: github.com/bluuewhale/loom/graph" bench-baseline.txt` — correct module
</verification>

<success_criteria>
All 7 code review items resolved, all Go tests pass, bench-baseline.txt regenerated with correct module name.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-id1-leiden-go-silent-discard-uniquecommuniti/260331-id1-SUMMARY.md`
</output>
